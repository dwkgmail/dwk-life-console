spring:
  datasource:
    url: jdbc:h2:mem:dwk;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true
app:
  admin:
    username: admin
    password: test-password
  token:
    secret: test-secret-long-enough-for-hmac-validation
    ttl-hours: 1
